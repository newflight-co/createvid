// import VueSocketIO from 'vue-socket.io'

export default async ({ Vue, store }) => {
  /*
  Vue.use(new VueSocketIO({
    debug: true,
    connection: '/',
    vuex: {
      store
    }
  }))
  */
}
